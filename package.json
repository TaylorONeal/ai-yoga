{
  "name": "ai-yoga-toolkit",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "build:pages": "node scripts/build-site.mjs --out .",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" }
}
