# Synthetic templates

`sample_data.csv` contains six invented rows in the year 2040, using Demo names and places. None represents actual attendance or training. `yoga_visits_template.xlsx` contains headings and summary formulas only, with no populated practitioner records. Keep real data outside the public repository.
